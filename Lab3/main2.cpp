/*
Write a C++ program that reads three integer values from the user (through keyboard entry), then order and display the three values in ascending order. For example, if the user entered 3 values: 84 3 130, the program should output the three values as: 3, 84, 130.

At the end of the program, the first value contains the smallest value, the second value contains the middle value, and the 3rd value has the largest value.
*/  
#include <iostream>
using namespace std;

int main() {

  int value1, value2, value3; // 3 values to be sorted

  // step 1: prompt for the 3 values
  cout << "Please enter the three integer values: ";
  cin >> value1 >> value2 >> value3;

  // step 2: Order the 3 values and display them in ascending order
  // Use proper decision statement and assignment statements to order the 3 values
  // If needed, you can look at hints on the course page.
  

  
  return 0;
}